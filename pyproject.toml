[project]
name = "two-wheel-exp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cvxpy>=1.9.1",
    "gymnasium>=1.3.0",
    "numpy>=2.4.6",
    "pygame>=2.6.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
